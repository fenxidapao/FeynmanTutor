# 字符串方法

- `s.upper()` 转大写、`s.lower()` 转小写（返回新字符串，不改原串）。
- `s.strip()` 去掉首尾空白；`s.split("x")` 按分隔符切成列表（如 `"a,b".split(",")` → `["a","b"]`）。
- `s.join(list)` 用 s 把列表元素连成字符串（如 `"-".join(["a","b"])` → `"a-b"`）。
- 注意：字符串方法大多**返回新值**，原字符串不变。
