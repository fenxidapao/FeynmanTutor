# 类型转换

- `int(x)` 把内容转成整数（`int("3")` → `3`；`int(3.7)` → `3` 截断）。
- `float(x)` 转浮点数（`float("3.5")` → `3.5`）。
- `str(x)` 把内容转成字符串（`str(42)` → `"42"`），常用于拼接时：`"年龄" + str(20)`。
- 转换失败会报 `ValueError`，如 `int("abc")`。
