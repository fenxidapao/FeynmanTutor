# 列表方法

- `a.append(x)` 在末尾加一个元素；`a.extend([...])` 追加一整个列表。
- `a.insert(i, x)` 在索引 i 处插入；`a.remove(x)` 删除第一个值等于 x 的元素。
- `a.pop()` 弹出并返回末尾元素，`a.pop(i)` 弹出索引 i 处；`a.sort()` 原地排序。
- 注意：`append` 是把整个对象作为一个元素加进去，`a.append([1,2])` 会得到一个嵌套列表。
